<UserControl
    x:Class="Tabstagram.ThumbnailControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tabstagram"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="150"
    d:DesignWidth="150">
    
    <Grid Name="MainGrid" Width="150" Height="150" Visibility="Collapsed">
        <Image x:Name="thumbnail" Source="{Binding images.thumbnail.url}" HorizontalAlignment="Center" 
                Margin="0,0,0,0" VerticalAlignment="Center" ImageOpened="thumbnail_ImageOpened"/>
        <Grid VerticalAlignment="Bottom">
            <Grid.Background>
                <SolidColorBrush Color="Black" Opacity="0.3"/>
            </Grid.Background>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Likes: " />
            <TextBlock Text="{Binding likes.count}" HorizontalAlignment="Right" />
        </Grid>
    </Grid>
</UserControl>
